<!DOCTYPE html>
<html>
  <head>
    <title>TakeHomeTest</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container my-3">
      <%= render 'layouts/breadcrumbs' if breadcrumbs.any? %>
      <%= render 'layouts/page_header' if content_for?(:page_title) || content_for?(:page_cta) %>
      <%= yield %>
    </div>
  </body>
</html>
